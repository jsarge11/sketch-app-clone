const initialState = {
    shapes: [],
    selected: {backgroundColor: '#0440e5', border: 2, borderColor: '#ba0000'}
}

export default function reducer(state = initialState, action){
    return state
}