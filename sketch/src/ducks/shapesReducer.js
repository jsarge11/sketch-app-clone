const initialState = {
    shapes: [],
    selected: {type: 'square', backgroundColor: '#0440e5', border: 2, borderColor: '#ba0000', boxShadow: '5px 5px 5px 5px #E30C0C', filter: 'blur(4px)'}
}

export default function reducer(state = initialState, action){
    return state
}