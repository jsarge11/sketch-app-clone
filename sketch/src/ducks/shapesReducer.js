const initialState = {
    shapes: [],
    selected: {backgroundColor: '#0440e5', border: 2, borderColor: '#ba0000', boxShadow: '5px 5px 5px 5px #E30C0C'}
}

export default function reducer(state = initialState, action){
    return state
}